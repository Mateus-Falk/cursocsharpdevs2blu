@model Devs2Blu.ProjetosAula.Atividade4MVC.Models.DTO.GeneroViewModel

@{
	Layout = null;
}

<div class="modal-dialog">
	<div class="modal-content">
		<div class="modal-header">
			<h5 class="modal-title">Editar Genero</h5>
			<button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
		</div>
		<div class="modal-body">
			<div id="content">
				<form asp-action="Edit">
					<div asp-validation-summary="ModelOnly" class="text-danger"></div>
					<input type="hidden" asp-for="id"/>
					<div class="from-group">
						<label asp-for="nome" class="control-label"></label>
						<input asp-for="nome" class="form-control">
						<span asp-validation-for="nome" class="text-danger"></span>
					</div>
					<div class="form-group">
						<input id="btnUpdateGenero" type="submit" value="Edit" class="btn btn-success">
					</div>
				</form>
			</div>
		</div>
	</div>
</div>

<script>

	/*$(document).ready(() => {
		$('#btnUpdateGenero').click(() => executeUpdateGenero());
	});

	const executeUpdateGenero = () => {
		$.ajax({
			url: `@Url.Action("Edit")`,
			method: 'POST',
			data: {
				id: @Model.id,
				nome: '@Model.nome'
			},
			succes: (response) => {
				if (response === "OK") {
					let htmlMsg=`   <div class="alert alert-succes" id="alertMsg" style="display: block;">
										<h2>Registro Editado com Sucesso!</h2>
									</div>`;
					$('#content').html(htmlMsg);
					$('#modal').show();
					$('#modal-footer').hide();
					$('#modal-header').hide();
					setTimeout(()=>{
						window.location.reload(1)
					},2000);
				}
			}
		})
	}
	*/
</script>
